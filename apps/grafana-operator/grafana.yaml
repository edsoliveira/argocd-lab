apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana-operator
  namespace: monitoring
spec:
  config:
    log:
      mode: console
      level: info

  ingress:
    enabled: true
    hostname: grafana-op.local
