apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana-op
  namespace: monitoring
  labels:
    dashboards: grafana
spec:
  config:
    log:
      mode: console
      level: info
    security:
      admin_user: admin
      admin_password: teste666
